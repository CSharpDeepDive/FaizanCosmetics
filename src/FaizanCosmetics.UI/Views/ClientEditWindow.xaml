<Window x:Class="FaizanCosmetics.UI.Views.ClientEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="620" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" Style="{StaticResource HeaderText}" FontSize="18" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,16,0,16">
            <StackPanel>
                <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ValidationMessageText}"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" Margin="0,0,0,10" />

                <TextBlock Text="This is the system Walk-in Customer record — some fields are locked."
                           Style="{StaticResource ValidationMessageText}" Foreground="{StaticResource WarningBrush}"
                           Visibility="{Binding IsWalkInCustomer, Converter={StaticResource BooleanToVisibilityConverter2}}" Margin="0,0,0,10" TextWrapping="Wrap" />

                <TextBlock Text="Client Code" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                <TextBox Text="{Binding ClientCode}" Style="{StaticResource AppTextBox}" IsReadOnly="True" Margin="0,0,0,12" />

                <TextBlock Text="Name *" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}"
                         IsEnabled="{Binding IsWalkInCustomer, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,12" />

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Phone" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Email" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}" />
                    </StackPanel>
                </Grid>

                <TextBlock Text="Address" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}"
                         AcceptsReturn="True" Height="56" TextWrapping="Wrap" Margin="0,0,0,12" />

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Client Type" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding ClientTypes}" SelectedItem="{Binding SelectedClientType}" Padding="8,6"
                                  IsEnabled="{Binding IsWalkInCustomer, Converter={StaticResource InverseBooleanConverter}}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Credit Limit" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding CreditLimit, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}" />
                    </StackPanel>
                </Grid>

                <StackPanel Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter2}, ConverterParameter=Invert}">
                    <TextBlock Text="Opening Balance (amount already owed before using this system, if any)" Style="{StaticResource BodyText}" Margin="0,0,0,4" TextWrapping="Wrap" />
                    <TextBox Text="{Binding OpeningBalance, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AppTextBox}" Margin="0,0,0,12" />
                </StackPanel>

                <StackPanel Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter2}}">
                    <TextBlock Text="Current Balance" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
                    <TextBox Text="{Binding CurrentBalance, StringFormat=N2}" Style="{StaticResource AppTextBox}" IsReadOnly="True" Margin="0,0,0,12" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" />
            <Button Grid.Column="2" Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />
        </Grid>
    </Grid>
</Window>
