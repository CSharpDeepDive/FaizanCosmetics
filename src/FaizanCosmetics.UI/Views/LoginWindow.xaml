<Window x:Class="FaizanCosmetics.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Faizan Cosmetics — Sign In"
        Height="520" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        xmlns:controls="clr-namespace:FaizanCosmetics.UI.Views.Controls">
    <Grid Margin="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Faizan Cosmetics" Style="{StaticResource HeaderText}" FontSize="26" HorizontalAlignment="Center" />
        <TextBlock Grid.Row="1" Text="Point of Sale &amp; Khata Management" Style="{StaticResource SubHeaderText}" HorizontalAlignment="Center" Margin="0,4,0,0" />

        <StackPanel Grid.Row="3">
            <TextBlock Text="Username" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
            <TextBox x:Name="UsernameTextBox" Style="{StaticResource AppTextBox}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="5">
            <TextBlock Text="Password" Style="{StaticResource BodyText}" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" Style="{StaticResource AppPasswordBox}" KeyDown="PasswordBox_KeyDown" />
        </StackPanel>

        <TextBlock Grid.Row="7" Text="{Binding ErrorMessage}" Style="{StaticResource ValidationMessageText}"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" TextWrapping="Wrap" />

        <Button Grid.Row="9" Content="Sign In" Style="{StaticResource PrimaryButton}" Click="SignInButton_Click"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />

        <StackPanel Grid.Row="11" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,0" Opacity="0.75">
            <controls:AetheriaLogoMark Width="18" Height="18" Margin="0,0,6,0" />
            <TextBlock VerticalAlignment="Center" Style="{StaticResource BodyText}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                <Run Text="Powered by " /><Run Text="Aetheria Labs" FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
